int printf(char* text, ...)
void* malloc(u64 size)

enum enumTempTest<T> {
	a(T)
	b(i32)
}

struct tempTest<T T2> {
	T a
	T2 b
}

void main(){
	enumTempTest<i64> enumTest = enumTempTest<i64>.a(5)

	tempTest<i32*, i32 tempTest<i32 i8>> test
	test.a = malloc(i32.size), 6
	test.a.0.val = 8
	u8 k = test.a.0.val 
	u16 m = enumTest.a 
	for uint j in m ... k {
		printf("%d\n", j)
	}
	printf("end")
}